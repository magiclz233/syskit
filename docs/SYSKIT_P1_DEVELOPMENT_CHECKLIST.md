# syskit P1 开发需求清单

## 1. 文档目的

这份文档是 `syskit` P1 阶段的唯一开发控制清单，用于管理以下事项：

1. P1 范围内需要开发的需求项。
2. 需求项的开发顺序、依赖关系和当前状态。
3. 每次开发完成后需要回写的完成标记。
4. 后续开发时默认继续处理的“下一个未完成需求”。

本清单对齐以下文档：

- `docs/SYSKIT_PRODUCT_PRD.md`
- `docs/SYSKIT_CLI_SPEC.md`
- `docs/SYSKIT_ARCHITECTURE.md`
- `docs/SYSKIT_RULES_CATALOG.md`

## 2. 使用规则

1. 本文档确认前，不进入正式 P1 开发。
2. 文档确认后，默认严格按本文档自上而下顺序开发。
3. 若某项未完成，默认不跳过到后续项；确需跳过时，需要先明确调整优先级。
4. 每完成一项开发并完成基本验证后，将状态更新为 `已开发`。
5. 下一次继续开发时，从第一个状态不是 `已开发` 的需求项继续。
6. 若某项拆分为多次提交，状态可暂记为 `开发中`，完成后再改为 `已开发`。

## 3. 状态约定

| 状态 | 含义 |
|---|---|
| `待确认` | 已整理但尚未得到用户确认，不开始开发 |
| `待开发` | 已确认但尚未开始 |
| `开发中` | 正在实现中 |
| `已开发` | 已完成实现并做过基本验证 |
| `暂缓` | 暂不继续，等待重新排期 |

当前整份清单状态：`开发中`

## 4. P1 范围边界

本清单只覆盖 P1，包含：

1. 网络工具与专项诊断：`port ping/scan`、`net`、`dns`、`ping`、`traceroute`、`doctor network/disk-full/slowness`。
2. 深度监控：`cpu burst/watch`、`mem leak/watch`、`monitor all`。
3. 运维对象：`service`、`startup`、`log` 命令组。
4. 文件治理增强：`file dup/archive/empty/dedup`。
5. 修复编排：`fix run` 与 YAML 剧本执行。
6. P1 规则集、P1 测试闭环与文档收口。

本清单不包含 P2 内容，例如：

1. 插件机制与自定义规则扩展框架。
2. 容器/K8s 诊断能力。
3. Web 可视化与多主机批量巡检。

## 5. 前置确认项

| 顺序 | ID | 状态 | 事项 | 说明 |
|---|---|---|---|---|
| 0 | `P1-PRE-001` | `已开发` | P0 基线闭环已完成 | 以 `docs/SYSKIT_P0_DEVELOPMENT_CHECKLIST.md` 为完成基线继续迭代 |

## 6. P1 开发清单

### 阶段 A：网络能力与专项诊断

| 顺序 | ID | 状态 | 需求项 | 关键交付 |
|---|---|---|---|---|
| 1 | `P1-001` | `已开发` | 实现 `port ping` | 支持 TCP 端口可达性测试、`--count/--timeout/--interval`、统一输出 |
| 2 | `P1-002` | `已开发` | 实现 `port scan` | 支持端口范围扫描、`--port/--mode/--timeout`、开放端口清单 |
| 3 | `P1-003` | `已开发` | 实现 `net conn/listen` | 提供连接审计与监听列表（与 `port` 能力形成互补） |
| 4 | `P1-004` | `已开发` | 实现 `dns resolve/bench` | 支持基础解析与 DNS 响应延迟基准测试 |
| 5 | `P1-005` | `已开发` | 实现 `ping/traceroute` | 支持主机连通性与路由链路诊断 |
| 6 | `P1-006` | `已开发` | 实现 `net speed` | 落地可解释的带宽测速流程与结果输出 |
| 7 | `P1-007` | `已开发` | 实现 `doctor network` | 打通网络专项采集、规则评估、退出码和降级协议 |
| 8 | `P1-008` | `已开发` | 实现 `doctor disk-full/slowness` | 落地场景化诊断编排与建议输出 |

### 阶段 B：监控与趋势分析

| 顺序 | ID | 状态 | 需求项 | 关键交付 |
|---|---|---|---|---|
| 9 | `P1-009` | `已开发` | 实现 `cpu burst` | 支持突发高 CPU 采样、阈值命中与证据输出 |
| 10 | `P1-010` | `已开发` | 实现 `cpu watch` | 支持持续监控、阈值告警与可中断输出 |
| 11 | `P1-011` | `已开发` | 实现 `mem leak <pid>` | 支持进程内存趋势采样和泄漏判定 |
| 12 | `P1-012` | `已开发` | 实现 `mem watch` | 支持系统内存持续观测和阈值告警 |
| 13 | `P1-013` | `已开发` | 实现 `monitor all` | 支持统一监控轮询、落盘与告警入口 |
| 14 | `P1-014` | `已开发` | 实现定时巡检和 webhook 基线 | 支持计划执行 `doctor all` 与告警投递 |

### 阶段 C：运维对象命令

| 顺序 | ID | 状态 | 需求项 | 关键交付 |
|---|---|---|---|---|
| 15 | `P1-015` | `待开发` | 实现 `service list/check` | 服务清单和健康检查（含平台差异降级） |
| 16 | `P1-016` | `待开发` | 实现 `service start/stop/restart` | 写操作保护、审计日志、可解释失败原因 |
| 17 | `P1-017` | `待开发` | 实现 `service enable/disable` | 危险操作门禁与审计落盘 |
| 18 | `P1-018` | `待开发` | 实现 `startup list` | 启动项采集、风险标记与策略联动 |
| 19 | `P1-019` | `待开发` | 实现 `startup enable/disable` | 危险操作门禁、回滚提示和审计 |
| 20 | `P1-020` | `待开发` | 实现 `log` | 日志快速体检与统计输出 |
| 21 | `P1-021` | `待开发` | 实现 `log search` | 关键字检索、上下文输出、范围过滤 |
| 22 | `P1-022` | `待开发` | 实现 `log watch` | 日志增长和错误率持续监控 |

### 阶段 D：文件治理与修复编排

| 顺序 | ID | 状态 | 需求项 | 关键交付 |
|---|---|---|---|---|
| 23 | `P1-023` | `待开发` | 实现 `file dup` | 重复文件检测、hash 策略和结果导出 |
| 24 | `P1-024` | `待开发` | 实现 `file archive` | 旧文件归档、压缩选项与保留策略 |
| 25 | `P1-025` | `待开发` | 实现 `file empty` | 空目录清理的 dry-run/apply 流程 |
| 26 | `P1-026` | `待开发` | 实现 `file dedup` | 重复文件清理与危险操作门禁 |
| 27 | `P1-027` | `待开发` | 实现 `fix run` | 执行剧本、失败策略、平台条件和审计接入 |

### 阶段 E：规则、质量与发布收口

| 顺序 | ID | 状态 | 需求项 | 关键交付 |
|---|---|---|---|---|
| 28 | `P1-028` | `待开发` | 实现 P1 规则集 | 至少落地 `NET-001`、`SVC-001`、`STARTUP-001`、`LOG-001` |
| 29 | `P1-029` | `待开发` | 打通 P1 报告与告警结构 | 模板化报告字段、监控告警输出协议 |
| 30 | `P1-030` | `待开发` | 补齐 P1 单元/集成测试 | 覆盖新命令参数、采集器、降级、规则与审计 |
| 31 | `P1-031` | `待开发` | 补齐契约测试与帮助校验 | 校验命令树、JSON 字段、示例和危险提示 |
| 32 | `P1-032` | `待开发` | 完成跨平台验证与文档收口 | 更新 README/QUICKSTART/DEV_GUIDE/发布脚本与验证清单 |

## 7. 当前推荐开发顺序

确认本清单后，推荐按以下大顺序推进：

1. 先做阶段 A，优先替换网络相关占位命令。
2. 再做阶段 B，形成持续监控数据链路。
3. 再做阶段 C，补齐服务/启动项/日志运维对象能力。
4. 再做阶段 D，完成文件治理与剧本编排闭环。
5. 最后做阶段 E，完成规则、测试、文档和发布收口。

## 8. 完成标记模板

每个需求项完成后，在对应行状态改为 `已开发`，并在本节追加记录：

```text
- 2026-03-27: `P1-001` 已开发，完成 `port ping` 及参数与输出协议接入。
```

开发记录：

- 2026-03-27: `P1-PRE-001` 已开发，确认 P0 基线闭环完成，P1 迭代从第一个未开发项开始。
- 2026-03-27: `P1-001` 已开发，`port ping` 从占位命令升级为正式命令，支持 `--count/--interval/--timeout` 参数，补齐 table/json/markdown/csv 输出与单元测试。
- 2026-03-27: `P1-002` 已开发，`port scan` 从占位命令升级为正式命令，支持 `--port/--mode/--timeout` 参数、quick/full 默认端口策略与扫描结果汇总，并补齐单元测试。
- 2026-03-27: `P1-003` 已开发，新增 `net` 命令组并正式交付 `net conn`、`net listen`，支持 `--pid/--state/--proto/--remote` 与 `--proto/--addr` 过滤，`net speed` 保持占位，补齐命令与采集器单元测试。
- 2026-03-27: `P1-004` 已开发，新增 `dns` 命令组并正式交付 `dns resolve`、`dns bench`，支持 `--type/--dns/--count/--timeout` 参数、结构化结果与时延统计，并补齐命令与采集器单元测试。
- 2026-03-27: `P1-005` 已开发，新增正式命令 `ping`、`traceroute`，落地跨平台系统命令探测、table/json/markdown/csv 输出与命令/采集器单元测试。
- 2026-03-28: `P1-006` 已开发，`net speed` 升级为正式测速命令，支持 `full/download/upload` 模式、阶段化测速明细、公网出口与运营商信息解析、可解释结论输出，并补齐命令与采集器单元测试。
- 2026-03-29: `P1-007` 已开发，新增 `doctor network` 正式命令，支持 DNS→Ping→Traceroute 链路诊断、`NET-*` 规则评估、`fail-on` 退出码和模块降级协议，并补齐单元/集成测试。
- 2026-03-29: `P1-008` 已开发，新增 `doctor disk-full`、`doctor slowness` 正式命令，打通场景化规则评估、`fail-on` 退出码、降级协议与 JSON 契约/集成测试。
- 2026-03-29: `P1-009` 已开发，新增 `cpu burst` 正式命令，支持 `interval/duration/threshold` 高频采样、突发命中证据聚合输出，并补齐单元/集成测试。
- 2026-03-29: `P1-010` 已开发，新增 `cpu watch` 正式命令，支持持续采样、`threshold-cpu/threshold-load` 阈值告警聚合、Ctrl+C/timeout 可中断退出与汇总输出，并补齐单元/集成测试。
- 2026-03-29: `P1-011` 已开发，新增 `mem leak <pid>` 正式命令，支持 `duration/interval` 趋势采样、RSS 增长斜率与泄漏风险分级，并补齐单元/集成测试。
- 2026-03-29: `P1-012` 已开发，新增 `mem watch` 正式命令，支持持续采样、`threshold-mem/threshold-swap` 阈值告警聚合、Ctrl+C/timeout 可中断退出与汇总输出，并补齐单元/集成测试。
- 2026-03-29: `P1-013` 已开发，新增 `monitor all` 正式命令，支持 CPU/MEM/DISK/PROC/NET 统一轮询、阈值连续命中告警聚合、JSONL 样本落盘到 `data_dir/monitor`，并补齐单元/集成测试。
- 2026-03-29: `P1-014` 已开发，在 `monitor all` 中支持 `inspection-interval` 定时触发 `doctor all` 与 webhook 告警推送基线，巡检结果纳入结构化输出并补齐集成测试。

## 9. 已确认事项

1. 本清单作为 P1 唯一开发顺序文档使用。
2. 默认按本文档顺序逐项开发，不随意跳项。
3. 已完成项必须同步更新状态和开发记录。


